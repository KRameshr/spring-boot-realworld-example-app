package io.spring.contract;

import io.specmatic.test.SpecmaticContractTest;

public class ContractTests implements SpecmaticContractTest {
  // empty implementation - baseUrl is already configured in specmatic.yaml (runOptions.localTest.openapi.baseUrl)
}